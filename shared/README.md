# shared

Types and constants shared between `frontend` and `backend` (e.g. queue
status enums, notification threshold config) live here once they're needed
by both sides. Empty for Milestone 1 — populated as Milestones 2+ introduce
duplicated shapes.